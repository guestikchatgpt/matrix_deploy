---
- name: Обновить кеш apt
  apt:
    update_cache: true
    cache_valid_time: 86400

- name: Установить базовые пакеты
  apt:
    name:
      - ca-certificates
      - curl
      - gnupg
      - lsb-release
      - python3
      - python3-pip
      - python3-venv
      - python3-psycopg2
      - software-properties-common
      - acl
      - openssl

- name: Установить Python SDK Docker из официального PyPI
  ansible.builtin.apt:
    name: python3-docker
    state: present

- name: Создать системную группу Matrix
  group:
    name: "{{ matrix_system_group }}"
    system: true

- name: Создать системного пользователя Matrix
  user:
    name: "{{ matrix_system_user }}"
    group: "{{ matrix_system_group }}"
    shell: /usr/sbin/nologin
    system: true
    create_home: false

- name: Создать корневую директорию стека
  file:
    path: "{{ matrix_root_dir }}"
    state: directory
    owner: "{{ matrix_system_user }}"
    group: "{{ matrix_system_group }}"
    mode: "0750"

- name: Создать директорию для секретов
  file:
    path: "{{ matrix_root_dir }}/.secrets"
    state: directory
    owner: "{{ matrix_system_user }}"
    group: "{{ matrix_system_group }}"
    mode: "0700"

- name: Map host architecture to Debian repository architecture
  ansible.builtin.set_fact:
    matrix_deb_arch: "{{ {'x86_64': 'amd64', 'aarch64': 'arm64'}[ansible_architecture] | default('') }}"

- name: Require a supported CPU architecture
  ansible.builtin.assert:
    that:
      - matrix_deb_arch | length > 0
    fail_msg: "Unsupported architecture for Matrix Deploy: {{ ansible_architecture }}"
